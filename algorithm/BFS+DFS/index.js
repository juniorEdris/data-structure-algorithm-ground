/**
 * 
 * 
 * 
//If you know a solution is not far from the root of the tree:
> BFS

//If the tree is very deep and solutions are rare: 
> BFS (DFS will take much time)

//If the tree is very wide:
> DFS (BFS will take much memory)

//If solutions are frequent but located deep in the tree:
> DFS

//Determining whether a path exists between two nodes:
> DFS

//Finding the shortest path:
> BFS
 * 
 * 
 */

import { BinarySearchTrees } from "../../dataStructures/Trees/BinarySearchTrees";

export function BDFSfunc() {
  console.log("BFS+DFS is completed!");
  //   const bfs = new BinarySearchTrees();

  //   bfs.insert(9);
  //   bfs.insert(4);
  //   bfs.insert(20);
  //   bfs.insert(1);
  //   bfs.insert(6);
  //   bfs.insert(15);
  //   bfs.insert(170);
  //   bfs.breadthFSearch();
  //   bfs.breadthFSearch([bfs.root], []);
  //   bfs.DFSInorder();
  //   console.log(bfs.DFSInorder());
  //   console.log(bfs.DFSPreorder());
  //   console.log(bfs.DFSPostorder());
}
